#!/sbin/openrc-run
# Original-Author: @integratec

name="NUT Webgui"
description="nut_webgui - Simple NUT Web interface"

depend() {
    use net
}

command="/usr/local/bin/nut_webgui"
command_args="--allow-env ${NUTWG_EXTRA_CMD_ARGS}"
required_files="${NUTWG_REQUIRED_FILES}"
pidfile="/run/${RC_SVCNAME}.pid"
command_background=true
